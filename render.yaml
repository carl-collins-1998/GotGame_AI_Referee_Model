services:
  - type: web
    name: basketball-referee-api
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.14
      - key: MODEL_URL
        sync: false
    autoDeploy: false